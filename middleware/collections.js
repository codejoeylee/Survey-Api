// middleware/attachCollections.js
/*const {
  getSurveyCollection,
  getResponseCollection,
} = require("../db/dbcollections.js");

function attachCollections(req, res, next) {
  req.collections = {
    survey: getSurveyCollection(),
  };
  next();
}

module.exports = attachCollections;
*/
